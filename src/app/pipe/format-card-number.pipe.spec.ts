import { FormatCardNumberPipe } from './format-card-number.pipe';

describe('FormatCardNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatCardNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
